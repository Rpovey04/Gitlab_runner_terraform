Need to define three variables:

- gitlab_runner_url: usually just https://gitlab.com
- gitlab_runner_token: obtained by creating a new runner on gitlab
- ssh_key_name: name of ssh key to add to instance. optionally just remove lines relating to key
